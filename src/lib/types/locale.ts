export type Dictionary = {
    companyName: string,
    components: {
        header: {
            title: string,
            github: string,
            discord: string
        },
        footer: {
            flatIcon: string,
            libreTranslate: string
        }
    }
}
